trigger:
- main

pool:
  vmImage: ubuntu-latest  # Self-hosted Agent 使う場合は後で名前変更

steps:
- checkout: self

- script: |
    cd terraform
    terraform init
    terraform plan
    terraform apply -auto-approve
  displayName: 'Run Terraform'